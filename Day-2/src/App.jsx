import React from 'react'    
// import React from 'react' : In modern React (using Vite), 
// this line is usually not required anymore. Older React versions needed it
//  because JSX was converted using the React object.Nowadays, 
// Vite and newer React versions handle that automatically.

const App = () => {
  return (
    <div>
      <h1>Hii there !</h1>
      <p>I'm learning React.</p>
    </div>
  )
}

export default App
